# API 基础URL
NEXT_PUBLIC_API_BASE_URL=http://host.docker.internal:8000
BACKEND_UPSTREAM=host.docker.internal:8000

# 应用配置
NEXT_PUBLIC_APP_NAME=听译加
NEXT_PUBLIC_APP_VERSION=1.0.0

# 轮询配置
NEXT_PUBLIC_POLLING_INTERVAL=2000
NEXT_PUBLIC_MAX_POLLING_TIME=3600000